# Secrets / env
.env
.env.*
*.local
local/secret_config.py

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
.vercel/

# Local data / receipts downloaded by the script
local/receipts/
local/transaction-backup.csv
local/printed-batches/
local/workingfund.db
local/pettycash.db
local/.env

# Runtime settings written by the review portal's Settings panel (may hold the
# MySQL password), so it stays out of git just like secret_config.py.
local/app_settings.json

# OS
.DS_Store
Thumbs.db

# Local scheduler lock
.claude/scheduled_tasks.lock
